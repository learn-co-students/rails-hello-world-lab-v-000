class StaticController < ApplicationController

  #have to make sure when creating controllers that the file name
  #seperated with an '_' in between the name and the world controller
  #in rails, I don't think you need to 'run' it inside of config

end