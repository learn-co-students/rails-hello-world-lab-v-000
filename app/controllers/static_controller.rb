class StaticController < ApplicationController
# Rails will automatically look inside the view directory
# with the same name as the controller (static)

	def hello_world
		# Implicit rendering
	end

end
